Pour compiler ce module, il faut avoir la bibliothèque STM32CubeF4 à la racine (au même niveau que le répertoire CodeSource).

Ensuite, il suffit de lancer le script "compile" dans le dossier du code source.
